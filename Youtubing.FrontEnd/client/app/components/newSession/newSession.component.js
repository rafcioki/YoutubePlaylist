import template from './newSession.html';
import controller from './newSession.controller';
import './newSession.scss';

let newSessionComponent = {
  restrict: 'E',
  bindings: {},
  template,
  controller
};

export default newSessionComponent;
